---
description: Start the Claude Code Dashboard server
---

Start the dashboard server using the serve.sh script.

Arguments ($ARGUMENTS) are passed as the port number (defaults to 8000 if not specified).

Run the serve script:
```bash
bash serve.sh $ARGUMENTS
```

If port is not specified, it defaults to 8000.
